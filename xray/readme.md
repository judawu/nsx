


说明文档见https://xtls.github.io/

confdir为服务器配置，client目录为客户端配置
运行 xray run -confdir="$DEST_DIR" -dump > "$XRAY_CONF 生成config.json
--------------------------------------------------------------------------------
xray wg

xray x25519

xray mlkem768

xray vlessenc

xray tls ping 

xray mldsa65

openssl rand -hex 4/8

protocol udp port: mkcp, hysteria, wireguard, ss

protocol tcp port: vless,vmess,trojan
